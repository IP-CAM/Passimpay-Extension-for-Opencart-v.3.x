{% if url %}
<div class="buttons">
    <div class="pull-right">
        <a href="{{ url }}" class="btn btn-primary">{{ button_confirm }}</a>
    </div>
</div>
{% elseif error %}
    <p class="alert-danger">{{ error }}</p>
{% endif %}
